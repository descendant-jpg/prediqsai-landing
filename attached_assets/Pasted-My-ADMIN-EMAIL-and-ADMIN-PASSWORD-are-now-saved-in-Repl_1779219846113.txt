My ADMIN_EMAIL and ADMIN_PASSWORD 
are now saved in Replit Secrets.

Do these steps right now:

1. Run this SQL in the database
   to give my account admin access:

UPDATE profiles 
SET is_admin = true 
WHERE email = (
  SELECT current_setting(
    'app.admin_email', true
  )
);

If that doesn't work try:
UPDATE profiles 
SET is_admin = true 
WHERE email = '{ADMIN_EMAIL}';

Replace {ADMIN_EMAIL} with the 
actual value from environment:
const adminEmail = 
  process.env.ADMIN_EMAIL;

await supabase
  .from('profiles')
  .update({ is_admin: true })
  .eq('email', adminEmail);

2. Run this admin setup script
   automatically on server start:

const setupAdmin = async () => {
  const adminEmail = 
    process.env.ADMIN_EMAIL;
    
  if (!adminEmail) {
    console.log('No admin email set');
    return;
  }
  
  const { data, error } = await 
    supabase
      .from('profiles')
      .update({ is_admin: true })
      .eq('email', adminEmail)
      .select();
      
  if (data && data.length > 0) {
    console.log(
      '✅ Admin activated:', 
      adminEmail
    );
  } else {
    console.log(
      '⚠️ Admin profile not found yet.',
      'Will activate on first login.'
    );
  }
};

setupAdmin();

3. Also add this to the login flow:
When any user logs in check if
their email matches ADMIN_EMAIL.
If yes automatically set 
is_admin = true:

const handleLogin = async (
  email, password
) => {
  const { data, error } = await
    supabase.auth.signInWithPassword({
      email,
      password
    });
    
  if (data.user) {
    // Auto-activate admin
    if (email === process.env.ADMIN_EMAIL) {
      await supabase
        .from('profiles')
        .update({ is_admin: true })
        .eq('email', email);
    }
    navigation.navigate('Dashboard');
  }
};

4. Show admin button in settings:
In the Settings screen check:
if (profile.is_admin === true) {
  // Show admin button
  <TouchableOpacity
    onPress={() => 
      navigation.navigate('AdminPanel')
    }
    style={adminButtonStyle}
  >
    <Text>🔐 Admin Panel</Text>
  </TouchableOpacity>
}

5. Confirm by showing me:
- Console log confirming admin activated
- The admin button visible in settings
- Admin panel overview screen

Do all of this now.