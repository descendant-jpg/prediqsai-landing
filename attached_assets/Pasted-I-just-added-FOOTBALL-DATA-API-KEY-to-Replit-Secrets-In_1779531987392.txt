I just added FOOTBALL_DATA_API_KEY 
to Replit Secrets.

Integrate this API throughout 
the app immediately.

API Details:
Base URL: https://api.football-data.org/v4
Header: X-Auth-Token: process.env.FOOTBALL_DATA_API_KEY

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
WHAT THIS API COVERS (FREE):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Free tier covers these competitions:
- World Cup 2026 (WC)
- Champions League (CL)
- Europa League (EL)
- Premier League (PL)
- Bundesliga (BL1)
- Serie A (SA)
- Ligue 1 (FL1)
- Eredivisie (DED)
- Primeira Liga (PPL)
- Championship (ELC)

Competition codes to use:
const competitions = {
  worldCup: 'WC',
  championsLeague: 'CL',
  europaLeague: 'EL',
  premierLeague: 'PL',
  bundesliga: 'BL1',
  serieA: 'SA',
  ligue1: 'FL1',
  eredivisie: 'DED',
  primeiraLiga: 'PPL',
  championship: 'ELC'
};

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
USE IT FOR THESE FEATURES:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. HEAD TO HEAD HISTORY:

const getHeadToHead = async (
  matchId
) => {
  const response = await fetch(
    https://api.football-data.org/v4/matches/${matchId}/head2head?limit=5,
    {
      headers: {
        'X-Auth-Token': 
          process.env.FOOTBALL_DATA_API_KEY
      }
    }
  );
  const data = await response.json();
  return data.matches || [];
};

Show last 5 H2H results:
Real Madrid 2-1 Barcelona (2024)
Barcelona 3-0 Real Madrid (2024)
Real Madrid 1-1 Barcelona (2023)
etc...

2. LEAGUE STANDINGS:

const getStandings = async (
  competition
) => {
  const response = await fetch(
    https://api.football-data.org/v4/competitions/${competition}/standings,
    {
      headers: {
        'X-Auth-Token': 
          process.env.FOOTBALL_DATA_API_KEY
      }
    }
  );
  const data = await response.json();
  return data.standings?.[0]?.table || [];
};

Use for Standings tab on
match detail page showing
full league table

3. TODAY'S MATCHES:

const getMatchesToday = async (
  competition
) => {
  const today = new Date()
    .toISOString().split('T')[0];
    
  const response = await fetch(
    https://api.football-data.org/v4/competitions/${competition}/matches?dateFrom=${today}&dateTo=${today},
    {
      headers: {
        'X-Auth-Token': 
          process.env.FOOTBALL_DATA_API_KEY
      }
    }
  );
  const data = await response.json();
  return data.matches || [];
};

4. TEAM SQUAD & PLAYERS:

const getTeamSquad = async (teamId) => {
  const response = await fetch(
    https://api.football-data.org/v4/teams/${teamId},
    {
      headers: {
        'X-Auth-Token': 
          process.env.FOOTBALL_DATA_API_KEY
      }
    }
  );
  const data = await response.json();
  return {
    squad: data.squad || [],
    coach: data.coach,
    venue: data.venue,
    founded: data.founded,
    colors: data.clubColors
  };
};

5. WORLD CUP 2026 MATCHES:

const getWorldCupMatches = async () => {
  const response = await fetch(
    'https://api.football-data.org/v4/competitions/WC/matches',
    {
      headers: {
        'X-Auth-Token': 
          process.env.FOOTBALL_DATA_API_KEY
      }
    }
  );
  const data = await response.json();
  return data.matches || [];
};

Use for World Cup section
showing all 104 matches with
real dates, venues and teams

6. MATCH DETAIL:

const getMatchDetail = async (
  matchId
) => {
  const response = await fetch(
    https://api.football-data.org/v4/matches/${matchId},
    {
      headers: {
        'X-Auth-Token': 
          process.env.FOOTBALL_DATA_API_KEY
      }
    }
  );
  return response.json();
};

Returns:
- Home and away team full names
- Score (half time and full time)
- Match status (SCHEDULED/LIVE/FINISHED)
- Venue name
- Referee name
- Goals with scorers and minute
- Bookings (yellow/red cards)
- Substitutions

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COMBINE WITH API-SPORTS:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Use BOTH APIs together for
maximum data coverage:

API-Sports (API_SPORTS_KEY):
✅ All 50+ leagues worldwide
✅ xG statistics
✅ Player ratings
✅ Injuries
✅ Live scores all leagues
✅ Odds data

Football-Data (FOOTBALL_DATA_API_KEY):
✅ Head to head history (better)
✅ Top 10 leagues (deeper data)
✅ Squad information
✅ Goal scorers
✅ Card details
✅ World Cup 2026 official data

Strategy:
- Use Football-Data for H2H,
  standings and squad data
- Use API-Sports for live scores,
  injuries, xG and all other leagues

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
UPDATE MATCH DETAIL PAGE:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

On the Prematch tab now show:

HEAD TO HEAD section:
Last 5 meetings between teams
fetched from Football-Data API:

H2H - Last 5 Meetings:
🏴󠁧󠁢󠁥󠁮󠁧󠁿 Arsenal vs Chelsea

May 2024: Arsenal 1-2 Chelsea
Jan 2024: Chelsea 0-5 Arsenal  
Oct 2023: Arsenal 1-1 Chelsea
Apr 2023: Chelsea 0-1 Arsenal
Jan 2023: Arsenal 3-1 Chelsea

Arsenal record vs Chelsea:
W3 D1 L1 in last 5 meetings

SQUAD section:
Show key players for both teams:
Home team top 5 players
Away team top 5 players
With positions and nationalities

REFEREE section:
Show referee name and stats:
"Referee: Michael Oliver"
Cards per game: 3.2
Penalties awarded: 0.8/game

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
UPDATE WORLD CUP SECTION:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Use Football-Data WC API to show:
- All 104 WC matches with real data
- Group stage fixtures
- Real venue names
- Actual kick off times
- Team logos from API

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ERROR HANDLING:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Football-Data free tier limits:
10 requests per minute

Add rate limiting protection:
const footballDataQueue = [];
let lastRequestTime = 0;

const fetchFootballData = async (url) => {
  const now = Date.now();
  const timeSinceLast = 
    now - lastRequestTime;
  
  if (timeSinceLast < 6000) {
    await new Promise(resolve => 
      setTimeout(resolve, 
        6000 - timeSinceLast)
    );
  }
  
  lastRequestTime = Date.now();
  
  const response = await fetch(url, {
    headers: {
      'X-Auth-Token': 
        process.env.FOOTBALL_DATA_API_KEY
    }
  });
  
  if (response.status === 429) {
    // Rate limited - wait and retry
    await new Promise(resolve => 
      setTimeout(resolve, 60000)
    );
    return fetchFootballData(url);
  }
  
  return response.json();
};

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TEST THE API FIRST:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Before using anywhere test that
the key works by calling:

GET https://api.football-data.org/v4/competitions
Header: X-Auth-Token: YOUR_KEY

Should return list of competitions.
If returns 403 the key is wrong.
If returns 200 the key works ✅

Log result to confirm:
console.log('Football Data API test:', 
  data.count, 'competitions available');

Integrate everything now and
show me the match detail page
with real H2H history showing
from Football-Data API.